{
  "name": "@remote-claude/websocket",
  "version": "0.1.0",
  "description": "WebSocket/Webhook service for Remote Claude",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "bun run src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "test": "bun test",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@remote-claude/ui": "workspace:*",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.23",
    "@types/node": "^20.10.4",
    "typescript": "^5.3.3"
  }
}