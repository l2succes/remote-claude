{
  "name": "@remote-claude/cli",
  "version": "0.1.0",
  "description": "CLI for Remote Claude",
  "bin": {
    "rclaude": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "bun run src/index.ts",
    "clean": "rm -rf dist",
    "test": "bun test",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@remote-claude/core": "workspace:*",
    "@remote-claude/config": "workspace:*",
    "@remote-claude/shared": "workspace:*",
    "@remote-claude/ui": "workspace:*",
    "commander": "^11.1.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.4",
    "@types/ws": "^8.5.10",
    "typescript": "^5.3.3"
  }
}